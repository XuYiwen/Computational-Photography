function output = texture_transfer(sample,target, patchsize,overlap,tol)
    % control the random sampling range
    r = floor (patchsize/2); 
    D = (2*r +1);
    result = zeros(size(target,1),size(target,2);
    
    
end
